"""
Integration tests test if software is correctly integrated with outside world,
e.g. if API server is responding.
"""

import pytest


def test_foo():
    pytest.fail("This placeholder integration test will always fail.")

